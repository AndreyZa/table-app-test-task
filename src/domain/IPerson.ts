// if someone also want create person code entity this interface can help
export interface IPerson {
  firstName: string;
  lastName: string;
  phone: string;
  gender: boolean; // male - true ; female -false
  age: number;
  id: number;
}
